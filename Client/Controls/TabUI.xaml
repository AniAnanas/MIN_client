<UserControl x:Class="Client.Controls.TabUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Controls"
             xmlns:vm="clr-namespace:Client.ViewModels" d:DataContext="{d:DesignInstance Type=vm:TabViewModel}"
             mc:Ignorable="d">

    <Border Width="280" Height="65"
            CornerRadius="10"
            Background="#FF2D2D30"
            BorderBrush="#FF3E3E40"
            BorderThickness="1"
            Cursor="Hand"
            MouseLeftButtonDown="Tab_Click">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0" Source="/content/Avatar.png" Margin="8"/>

            <Grid Grid.Column="1" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                        Text="{Binding Title}"
                        d:Text="User FullName" 
                        Foreground="#FFFFFF" 
                        VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" 
                        Text="{Binding LastMessage.Text}" 
                        d:Text="Last Message" 
                        Foreground="{StaticResource MutedBrush}" 
                        FontSize="12"/>

            </Grid>


            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                        Text="{Binding Time}" d:Text="12:00"
                        Foreground="#FFFFFF" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center" 
                        FontSize="10"
                        Margin="0,0,10,0"/>

                <Ellipse Grid.Row="1" 
                        Width="15" 
                        Height="15"
                        Fill="#D9D9D9" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center" 
                        Margin="7"
                        Visibility="{Binding HasUnread, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <TextBlock Grid.Row="1" 
                        Text="{Binding UnreadCount}" d:Text="99"
                        TextAlignment="Center" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center" 
                        FontSize="10"
                        Margin="7"
                        Visibility="{Binding HasUnread, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>

        </Grid>
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="#FF777777"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
    </Border>
</UserControl>
