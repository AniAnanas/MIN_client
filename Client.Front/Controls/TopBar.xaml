<UserControl x:Class="Client.Front.Controls.TopBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Front.Controls"
             mc:Ignorable="d" 
             d:Width="300"
             d:DesignHeight="30">

    <Border Background="Transparent" 
            CornerRadius="10,10,0,0"
            MouseLeftButtonDown="Border_MouseLeftButtonDown" VerticalAlignment="Center" >

        <Grid Height="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            
            <Image Grid.Column="0" Source="/content/icon1.png" Margin="7,0,0,0"/>

            <TextBlock Grid.Column="1" Text="MIN" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontFamily="Times New Roman"/>

            <!-- Кнопки управления -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button x:Name="MinimizeBtn" 
                    Content="─" 
                    Style="{StaticResource TitleButton}"
                    Click="MinimizeBtn_Click"
                />

                <Button x:Name="MaximizeBtn"
                    Content="=" 
                    Style="{StaticResource TitleButton}"
                    Click="MaximizeBtn_Click"
                />

                <Button x:Name="CloseBtn" 
                    Content="✕" 
                    Style="{StaticResource CloseButton}"
                    Click="CloseBtn_Click"
                />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
