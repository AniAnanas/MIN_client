<UserControl x:Class="Client.Front.Controls.Chat"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Front.Controls"
             xmlns:models="clr-namespace:Client.Front.Models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#FF1E1E1E">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Сообщения -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:Message}">
                        <Border Margin="0,4" 
                                CornerRadius="12" 
                                MaxWidth="400">
                            <StackPanel Margin="12,8">
                                <TextBlock Text="{Binding Sender}" 
                                           Foreground="#AAAAAA" 
                                           FontSize="10"/>
                                <TextBlock Text="{Binding Text}" 
                                           Foreground="White" 
                                           TextWrapping="Wrap"
                                           FontSize="14"/>
                                <TextBlock Text="{Binding Time, StringFormat='HH:mm'}" 
                                           Foreground="#777777" 
                                           FontSize="9"
                                           HorizontalAlignment="Right"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Панель ввода сообщения -->
        <Border Grid.Row="1" 
                Background="#4C000000"
                Padding="10"
                Margin="12"
                CornerRadius="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Image Grid.Column="0" Source="/Content/Skrepka.png" Width="Auto" Height="20" HorizontalAlignment="Left" Margin="5,0,5,0" />

                <TextBox Grid.Column="1" Text="Сообщение..." Name="Input_Area"
                         Foreground="White" FontSize="9" VerticalAlignment="Center" 
                         TextChanged="TextBox_TextChanged" KeyDown="TextBox_KeyDown">
                    <!-- TextChanged для отправки is typing...;  KeyDown для отправки/новой строки -->
                    
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Text" Value=""/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="False">
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Text" Value=""/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Grid.Column="2" Width="20" Height="20" 
                        HorizontalAlignment="Right" Margin="5,0,5,0" 
                        Background="Transparent" BorderBrush="Transparent">
                    <Image Source="/Content/SendArrow.png"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>